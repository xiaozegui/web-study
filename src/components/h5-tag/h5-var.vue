<template>
	<el-card>
		<h4>HTML &lt;var&gt; 标签</h4>
		<h4>定义和用法</h4>
		<p>&lt;var&gt; 标签表示变量的名称，或者由用户提供的值。</p>
		<p>&lt;var&gt; 标签是计算机文档中应用的另一个小窍门，这个标签经常与 &lt;code&gt;和 &lt;pre&gt; 标签一起使用，用来显示计算机编程代码范例及类似方面的特定元素。</p>
		<p>用 &lt;var&gt; 标签标记的文本通常显示为斜体。</p>
		<p>就像其他与计算机编程和文档相关的标签一样，&lt;var&gt; 标签不只是让用户更容易理解和浏览你的文档，而且将来某些自动系统还可以利用这些恰当的标签，从你的文档中提取信息以及文档中提到的有用参数。我们再一次强调，提供给浏览器的语义信息越多，浏览器就可以越好地把这些信息展示给用户。</p>
	</el-card>
</template>

<script>
	import codecard from '../code-card.vue'
	export default {
		data() {
			return {
				
			}
		},
		components: {
			'code-card': codecard
		}
	}
</script>

<style scoped="scoped">
</style>


