<template>
	<el-card>
		<h4>HTML &lt;canvas&gt; 标签</h4>
		<p><strong>实例</strong></p>
		<p>如何通过 canvas 元素来显示一个红色的矩形：</p>
		<code-card :mytextarea='canvas_text1' uniquename='canvas1'></code-card>
		<el-divider></el-divider>
		<h4>浏览器支持</h4>
		<p>Internet Explorer 9+, Firefox, Opera, Chrome 以及 Safari 支持 &lt;canvas&gt; 标签。</p>
		<p>注释：Internet Explorer 8 以及更早的版本不支持 &lt;canvas&gt; 标签。</p>
		<el-divider></el-divider>
		<h4>定义和用法</h4>
		<p>&lt;canvas&gt; 标签定义图形，比如图表和其他图像。</p>
		<p>&lt;canvas&gt; 标签只是图形容器，您必须使用脚本来绘制图形。</p>
		<el-divider></el-divider>
		<h4>HTML 4.01 与 HTML 5 之间的差异</h4>
		<p>&lt;canvas&gt; 标签是 HTML 5 中的新标签。</p>
		<el-divider></el-divider>
		<h4>属性</h4>
		<p><i class="el-icon-time"></i>HTML5 中的新属性。</p>
		<el-table :data="canvas_text2" style="width: 100%" :stripe="true" :border="true">
			<el-table-column label="属性">
				<template slot-scope="scope">
					<el-link src="" type="primary">{{ scope.row.attr }}&nbsp;&nbsp;<i class="el-icon-time" v-show="scope.row.isshow"></i></el-link>
				</template>
			</el-table-column>
			<el-table-column label="值">
				<template slot-scope="scope">
					<span>{{scope.row.value}}</span>
				</template>
			</el-table-column>
			<el-table-column label="描述">
				<template slot-scope="scope">
					<span>{{scope.row.desc}}</span>
				</template>
			</el-table-column>
		</el-table>
		<el-divider></el-divider>
		<h4>全局属性</h4>
		<p>&lt;canvas&gt;标签支持<el-link type='primary' href="/attribute">HTML5全局属性</el-link></p>
		<el-divider></el-divider>
		<h4>事件属性</h4>
		<p>&lt;canvas&gt;标签支持<el-link type='primary' href='/event'>全局事件属性</el-link> </p>
		<el-divider></el-divider>
		<h4>&lt;canvas&gt; 的历史</h4>
		<p>这个 HTML 元素是为了客户端矢量图形而设计的。它自己没有行为，但却把一个绘图 API 展现给客户端 JavaScript 以使脚本能够把想绘制的东西都绘制到一块画布上。</p>
		<p>&lt;canvas&gt; 标记由 Apple 在 Safari 1.3 Web 浏览器中引入。对 HTML 的这一根本扩展的原因在于，HTML 在 Safari 中的绘图能力也为 Mac OS X 桌面的 Dashboard 组件所使用，并且 Apple 希望有一种方式在 Dashboard 中支持脚本化的图形。</p>
		<p>Firefox 1.5 和 Opera 9 都跟随了 Safari 的引领。这两个浏览器都支持 &lt;canvas&gt; 标记</p>
		<el-divider></el-divider>
		<h4>&lt;canvas&gt; 标记和 SVG 以及 VML 之间的差异</h4>
		<p>&lt;canvas&gt; 标记和 SVG 以及 VML 之间的一个重要的不同是，&lt;canvas&gt; 有一个基于 JavaScript 的绘图 API，而 SVG 和 VML 使用一个 XML 文档来描述绘图。</p>
		<p>这两种方式在功能上是等同的，任何一种都可以用另一种来模拟。从表面上看，它们很不相同，可是，每一种都有强项和弱点。例如，SVG 绘图很容易编辑，只要从其描述中移除元素就行。</p>
		<p>要从同一图形的一个 &lt;canvas&gt; 标记中移除元素，往往需要擦掉绘图重新绘制它</p>
		<el-divider></el-divider>
		<h4>如何使用 &lt;canvas&gt; 标记绘图</h4>
		<p>大多数 Canvas 绘图 API 都没有定义在 &lt;canvas&gt; 元素本身上，而是定义在通过画布的 getContext() 方法获得的一个“绘图环境”对象上。</p>
		<p>Canvas API 也使用了路径的表示法。但是，路径由一系列的方法调用来定义，而不是描述为字母和数字的字符串，比如调用 beginPath() 和 arc() 方法。</p>
		<p>一旦定义了路径，其他的方法，如 fill()，都是对此路径操作。绘图环境的各种属性，比如 fillStyle，说明了这些操作如何使用。</p>
		<p>注释：Canvas API 非常紧凑的一个原因上它没有对绘制文本提供任何支持。要把文本加入到一个 &lt;canvas&gt; 图形，必须要么自己绘制它再用位图图像合并它，或者在 &lt;canvas&gt; 上方使用 CSS 定位来覆盖 HTML 文本。</p>
	</el-card>
</template>

<script>
	import codecard from '../code-card.vue'
	export default {
		data() {
			return {
				canvas_text1:'<!DOCTYPE HTML>\n<html>\n<body>\n\n<canvas id="myCanvas">your browser does not support the canvas tag </canvas>\n\n<script type="text/javascript">\n\nvar canvas=document.getElementById("myCanvas");\nvar ctx=canvas.getContext("2d");\nctx.fillStyle="#FF0000";\nctx.fillRect(0,0,80,100);\n\n\<\/script\>\n\n</body>\n</html>\n',
				canvas_text2:[
					{'attr':'height','value':'pixels','desc':'设置 canvas 的高度。','isshow':true},
					{'attr':'width','value':'pixels','desc':'设置 canvas 的宽度。','isshow':true}
				]
			}
		},
		components: {
			'code-card': codecard
		}
	}
</script>

<style scoped="scoped">
</style>



